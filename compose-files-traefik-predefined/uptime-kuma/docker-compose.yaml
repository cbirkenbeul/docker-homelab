---
version: '3.3'

services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: uptime-kuma
    volumes:
      - /var/docker/uptime-kuma/config:/app/data
    ports:
      - 3001:3001